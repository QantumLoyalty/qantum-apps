name: qantum_apps
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.


###################################
# STAR REWARD
#version:  1.0.9+103
###################################
# SENSE OF TASTE
# version:  1.0.0+56
###################################
# MAX
#version:  1.0.1+63
###################################
# HOGAN'S REWARD
#version: 1.0.5+16
###################################
# MHBC
#version:  1.0.14+102
###################################
# MONTAUK TAVERN
#version:  1.0.4+56
###################################
# CENTRAL LANE HOTEL
#version:  1.0.5+66
###################################
# NORTH SHORE TAVERN
#version: 1.0.4+12
###################################
#QANTUM
#version: 1.0.0+71
###################################
# ACE REWARDS
#version: 1.0.0+13
###################################
# QUEENS HOTEL
#version: 1.0.0+2
###################################
# BRISBANE BREWING
#version: 1.0.4+10
###################################
# WOOLLAHRA HOTEL
#version: 1.0.0+2
###################################
# Bluewater Captains Club
#version: 1.0.6+11
###################################
# FLINDERS STREET WHARVES
#version: 1.0.3+12
###################################
# Kingscliff
#version: 1.0.0+6
###################################
# drinkRewards
#version: 1.0.0+7
###################################
#QANTUMClub
#version: 1.0.0+3
###################################
#MAX Club
#version: 1.0.0+12
###################################
#WONTHAGGI
#version: 1.0.0+4
###################################
#EDP
version: 1.0.5+11
################################.###
#SOT
#version: 1.21.0+1205
###################################
#Bobs
#version: 1.23.0+1
###################################
#Mosaic Hotel
#version: 1.0.4+1
#Mannum club
#version: 1.0.0+3
#Southport Sharks
#version: 1.0.0+4




environment:
  sdk: ^3.5.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter



  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  carousel_slider: ^5.0.0
  screen_brightness: ^2.1.1
  secure_content: ^1.0.1
  flutter_portal: ^1.1.4
  package_info_plus: ^8.2.1
  #  flutter_background_geolocation: ^4.16.4
  flutter_local_notifications: ^18.0.1
  flutter_fortune_wheel: ^1.3.2
  http: ^1.2.2
  shared_preferences: ^2.2.3
  firebase_messaging: ^15.1.6
  firebase_core: ^3.9.0
  country_code_picker: ^3.1.0
  intl:
  syncfusion_flutter_gauges:
  flutter_staggered_grid_view: ^0.7.0
  syncfusion_flutter_charts: ^28.1.38
  countup: ^0.1.4
  fl_chart: ^0.70.1
  cached_network_image: ^3.3.1
  flutter_html:
  flutter_timezone: ^4.1.0
  path_provider: ^2.1.5
  onesignal_flutter: ^5.1.2
  simple_ripple_animation: ^0.1.0
  flutter_arc_text: ^0.6.0
  confetti: ^0.8.0
  lottie:
  webview_flutter: ^4.13.0
  condition_builder:
  json_annotation: ^4.9.0
  flutter_secure_storage: ^9.2.4
  flutter_localization: ^0.3.3
  # flutter_credit_card_scanner: ^0.11.9
  permission_handler: ^12.0.1
  image: ^4.2.0
  http_parser: ^4.1.2
  #flutter_camera_overlay: ^0.1.1
  flutter_camera_overlay_new: ^0.0.1
  camera: ^0.10.5+2
  #camera: ^0.11.3
  flutter_flip_card: ^0.0.6
  flutter_stripe: 12.4.0
#  easy_geofencing: ^0.2.0
  flutter_dotenv: ^6.0.0
  app_links:
  connectivity_plus: ^7.0.0
  flutter_custom_tabs: ^2.5.0
  sms_autofill: ^2.4.1
  syncfusion_flutter_barcodes:
  url_launcher:
  gif_view: ^1.0.3
  flutter_scratch_card: ^1.0.1
  audioplayers: ^6.7.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_slidable: ^4.0.3




dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  json_serializable: ^6.9.5


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/audio/
    - assets/common/
    - assets/starReward/
    - assets/max/
    - assets/hogansReward/

    - .env.mhbc
    - .env.maxc
    - .env.qantum
    - .env.kc
    - .env.dr
    - .env.mc
    - .env.ss
    - .env.edp

      # flavor-wise assets
  #  - path: assets/starReward/
  #    flavors:
  #        - starRewards

    - path: assets/senseOfTaste/
      flavors:
          - senseOfTaste

    - path: assets/qantum/
      flavors:
              - qantum

    - path: assets/qantumClub/
      flavors:
              - qantumClub

    - path: assets/maxClub/
      flavors:
              - maxClub

    - path: assets/manlyHarbourBoatClub/
      flavors:
              - mhbc

    - path: assets/centralLaneHotel/
      flavors:
              - clh

    - path: assets/montaukTavern/
      flavors:
              - montaukTavern

    - path: assets/northShoreTavern/
      flavors:
              - northShoreTavern

    - path: assets/aceRewards/
      flavors:
              - aceRewards

    - path: assets/brisbaneBrewing/
      flavors:
              - brisbane

    - path: assets/woollahraHotel/
      flavors:
              - woollahra

    - path: assets/bluewaterCaptainsClub/
      flavors:
              - bluewater

    - path: assets/flindersStreetWharves/
      flavors:
              - flinders

    - path: assets/kingscliff/
      flavors:
              - kingscliff

    - path: assets/drinkRewards/
      flavors:
              - drinkRewards

    - path: assets/wonthaggi/
      flavors:
              - wonthaggi

    - path: assets/edp/
      flavors:
              - edp

    - path: assets/bobsBulkBooze/
      flavors:
              - bobsBulkBooze

    - path: assets/mosaicHotel/
      flavors:
              - mosaic

    - path: assets/theMannumClub/
      flavors:
              - mannumClub

    - path: assets/southportSharks/
      flavors:
        - southportSharks


  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
     - family: Impact
       fonts:
         - asset: assets/fonts/impact.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
